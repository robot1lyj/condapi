# CLAUDE.md

本仓库的唯一项目规则入口是 `AGENTS.md`。宿主已注入时不要重复读取；按当前任务直接查对应 owner 章节，只有无法定位 owner 时查 `docs/cache/context_index.md`。`docs/cache/kernel.md`、mode、`docs/00_handoff_index.md` 和历史报告均按需读取，不作固定启动链。

本文件不保存模型状态、训练配方或第二套记忆。当前状态和操作边界以 `AGENTS.md` 与其指向的编号文档为准。
