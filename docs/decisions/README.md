# Decisions

Stable design decisions for this repository live here when they are too specific for hot context and more durable than a changelog entry.

Current default: use `AGENTS.md` plus `docs/cache/` as the only project memory system. Do not add parallel assistant memory trees.
