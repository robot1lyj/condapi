schema_version = 1
manager = "conda"
model = "pi"
target = "thor"
prefix = "/home/wuyan-lyj/.conda/envs/vla-pi-infer"
spec = "environments/bootstrap.yml"
# Proposed isolated environment, NOT the existing validated Pi Docker image.
