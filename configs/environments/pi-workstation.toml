schema_version = 1
manager = "conda"
model = "pi"
target = "workstation"
prefix = "/home/wuyan-lyj/.conda/envs/vla-pi-dev"
spec = "environments/bootstrap.yml"
# Bootstrap not installed or audited. Model dependency lock is a separate step.
