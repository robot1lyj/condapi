schema_version = 1
id = "evo1"
backend = "lerobot"
policy_type = "evo1"
status = "planned"
contracts = []
note = "Native LeRobot dependency lock and CPU environment checks are recorded separately; real YAM training and Thor inference remain unintegrated."
