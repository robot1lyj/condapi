schema_version = 2
operation = "train"
model = "openwam"
dataset = "configs/datasets/yam-lerobot-joints.toml"
split_manifest = "/path/to/new-yam-split/manifest.json"
algorithm = "configs/algorithms/openwam-joint-full.toml"
environment = "configs/environments/openwam-server.toml"
contract = "configs/robots/yam.toml"

[overrides]
native_config = "/path/to/audited-openwam-recipe.json"
