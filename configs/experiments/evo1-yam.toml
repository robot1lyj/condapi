schema_version = 1
model = "evo1"
environment = "configs/environments/evo1-workstation.toml"
contract = "configs/robots/yam.toml"

[parameters]
native_config = "/path/to/evo1-yam-train.json"
# Intentionally cannot run until a real YAM adapter and entrypoint are registered.
