schema_version = 1
model = "pi"
environment = "configs/environments/pi-server.toml"
contract = "configs/robots/yam.toml"

[parameters]
config = "pi05_yam_lora"
dataset = "/path/to/audited-yam-lerobot"
