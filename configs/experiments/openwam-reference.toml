schema_version = 1
model = "openwam"
environment = "configs/environments/openwam-server.toml"
contract = "configs/robots/yam.toml"

[parameters]
checkpoint = "/path/to/native/checkpoint-run"
request = "configs/requests/pi-example.json"
model_version = "openwam-yam-candidate"
