channels:
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge
dependencies:
  - python=3.12
  - pip
  - ffmpeg=7
# Install the pinned LeRobot molmoact2/training wheels separately.
# Do not install root OpenPI dependencies here.
